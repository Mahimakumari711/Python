class Library:
    def __init__(self):
        self.books = []
    
    def add_book(self, book):
        self.books.append(book)
    
    def remove_book(self, book):
        if book in self.books:
            self.books.remove(book)

        else:
            print(book, "not present in library")
    
    def display_books(self):
        print("Books: ", self.books)

library = Library()
book = input("Enter a book to add: ")
library.add_book(book)
book = input("Enter a book to remove: ")
library.remove_book(book)
library.display_books()

Output :

Enter a book to add:  Beloved, Canvas of life, Eight Lives
Enter a book to remove:  Eight Lives
Eight Lives not present in library
Books:  ['Beloved, Canvas of life, Eight Lives']
