class Student:
    def __init__(self, name, marks):
        self.name = name
        self.marks = marks
    def average_marks(self):
        return sum(self.marks) / len(self.marks)
    def grade(self):
        avg = self.average_marks()
        if avg >= 90:
            return "A"
        elif avg >= 75:
            return "B"
        elif avg >= 50:
            return "C"
        else:
            return "F"
    def high_grad(self):
        return max(self.marks)
        
name = input("Enter student name: ")
marks = list(map(int, input("Enter marks of student : ").split()))
student = Student(name, marks)
print("Marks:", student.marks)
print("Average Marks:", student.average_marks())
print("Grade:", student.grade())
print("Highest Marks:", student.high_grad())

Output :

Enter student name:  Ram Kumar
Enter marks of student :  90 87 89 98 88
Marks: [90, 87, 89, 98, 88]
Average Marks: 90.4
Grade: A
Highest Marks: 98
