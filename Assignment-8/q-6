class Employee :
    def __init__(self,name,salary) :
        self. name= name
            self.salary=salary
            
    def increase_salary(percent) :
        return self.salary=self.salary*(percent/100)
    
n=input("Enter the name of Employee : ")
b=int(input("Enter the salary : "))
per=int(input("Enter the percent to increase the salary : "))

print("Salary after incrasing by ",per,"% is ",)
