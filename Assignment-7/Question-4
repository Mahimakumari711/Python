def palindrome(num) :
    temp=num
    rev=0
    while temp!=0 :
        rem=temp%10
        rev=rev*10+rem
        temp=temp//10
    if(rev==num) :
        print("True") 
    else :
        print("False")

n=int(input("Enter the number : "))
palindrome(n)


Output :
Enter the number : 121
True
