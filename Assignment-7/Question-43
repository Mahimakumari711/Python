from functools import reduce
import ast

num = ast.literal_eval(input("Enter the values : "))
result = reduce(lambda x, y: x + y, 
                map(lambda x: x ** 3, 
                    filter(lambda x: x % 2 == 0, num)))
print(result)


Output :
Enter the values :  1,2,3,4,5,6,7
288
