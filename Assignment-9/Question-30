import sqlite3

conn = sqlite3.connect('college.db')
cursor = conn.cursor()

cursor.execute(""" 
    CREATE TABLE IF NOT EXISTS students (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        name TEXT NOT NULL,
        Marks INTEGER
    )""" )
conn.commit()
conn.close()

print("college.db created successfully!")

Output :
college.db created successfully!
