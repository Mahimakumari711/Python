arr = input("Enter array elements: ").split()

def integer(arr, idx):
    try:
        return arr[idx]
    except TypeError:
        return "Index must be integer"

idx = input("Enter index: ")
print(integer(arr, idx))

Output :
Enter array elements: 12 34 56 78 90
Enter index: k
Index must be integer



arr = input("Enter the element of array : ").split()

def index(arr, i):
    try :
        return arr[i]
    except IndexError :
        return "Index out of range."
i = int(input("Enter the index to access the element : "))
print(index(arr, i))

Output :
Enter the element of array : 12 34 56 78 90
Enter the index to access the element : 9
Index out of range.




filename = input("Enter filename: ")

try:
    f = open(filename, "r")
    content = f.read()
    f.close()
    print("File content:", content)
except FileNotFoundError:
    print(f"Error: File '{filename}' not found!")

print("Programm completed ")

Output :

Enter filename: Demo
Error: File 'Demo' not found!
Programm completed 





try : 
    a = int(input("Enter the first number : "))
    b = int(input("Enter the second number : "))
    
    a = a/b;
    print("Result of Division is ",a)
except ZeroDivisionError :
    print("Division by zero ")
except ValueError :
    print("Enter the integer")

Output :

Enter the first number : 12 
Enter the second number : L
Enter the integer


-------------------------------------------------------------------------------------------------------------------------------
Write a program that calculates the reciprocal of a number provided by the user. Use try, except for potential errors (ValueError, ZeroDivisionError), an else block to print the result of successful, and a finally block that always prints a cleanup message.

-------------------------------------------------------------------------------------------------------------------------------
try:
    num = float(input("Enter a number: "))
except ValueError:
    print("Error: Please enter a valid number")
else:
    try:
        reciprocal = 1 / num
        print("Reciprocal:", reciprocal)
    except ZeroDivisionError:
        print("Error: Cannot calculate reciprocal of zero")
finally:
    print("Calculation complete - cleanup done!")

Output :

Enter a number:  0
Error: Cannot calculate reciprocal of zero
Calculation complete - cleanup done!

