balance = float(input("Enter initial balance: "))
MIN_BALANCE = 1000
def deposit(amount):
    global balance
    if amount > 0:
        balance += amount
        print("Deposited:", amount)
    else:
        print("Invalid deposit amount")

def withdraw(amount):
    global balance
    try:
        if amount <= 0:
            raise Exception("Invalid withdrawal amount!")
        if balance - amount < MIN_BALANCE:
            raise Exception("Minimum balance of Rs.1000 must be maintained!")
        balance -= amount
        print("Withdrawn:", amount)
    except Exception as e:
        print("Error:", e)

while True:
    print("\n1. Deposit")
    print("2. Withdraw")
    print("3. Check Balance")
    print("4. Exit")
    
    choice = input("Enter your choice: ")
    if choice == '1':
        amt = float(input("Enter amount to deposit: "))
        deposit(amt)
    elif choice == '2':
        amt = float(input("Enter amount to withdraw: "))
        withdraw(amt)
    elif choice == '3':
        print("Current Balance:", balance)
    elif choice == '4':
        print("Exiting...")
        break
    else:
        print("Invalid choice!")


Output :

Enter initial balance:  45000

1. Deposit
2. Withdraw
3. Check Balance
4. Exit
Enter your choice:  3
Current Balance: 45000.0

1. Deposit
2. Withdraw
3. Check Balance
4. Exit
Enter your choice:  4
Exiting...
