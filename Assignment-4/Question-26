strings = input("Enter strings separated by space: ").split()
count = 0
for s in strings:
    if len(s) >= 2 and s[0] == s[-1]:
        count += 1
print(f"Number of matching strings: {count}")


Output :
Enter strings separated by space: pop top hot non non 
Number of matching strings: 3
